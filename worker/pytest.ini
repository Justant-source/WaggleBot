[pytest]
markers =
    unit: 외부 서비스 불필요 순수 단위 테스트
    requires_ffmpeg: FFmpeg가 설치된 환경에서만 실행 (Docker 권장)
    requires_db: DATABASE_URL 환경변수가 필요한 DB 테스트
    requires_fish: Fish Speech 서버가 실행 중이어야 하는 TTS 테스트
